const { resolveNodeSeo } = require("./resolve-seo");
const { resolveNodeImages } = require("./resolve-images");
const { resolveStoryblokLinks } = require("./resolve-storyblok-links");

module.exports = {
    resolveNodeSeo,
    resolveNodeImages,
    resolveStoryblokLinks,
};
